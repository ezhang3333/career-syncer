# Model Classifier — Uncertain Decisions

Entries are only logged when the pre-flight classifier had **low confidence** on a routing decision.

Mark correct entries ✅, incorrect ones ❌. Once a pattern recurs 3+ times and is consistently correct, it's stable — you can delete those entries. Use persistent misclassifications to update the classifier prompt in `CLAUDE.md`.

---

<!-- Format: - [ ] **YYYY-MM-DD** "task summary" → tier (reason) -->
